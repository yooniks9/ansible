version: '3.1'

services:

  cache:
    container_name: memcached
    image: memcached:latest
    restart: unless-stopped
    environment:
      TZ: "Asia/Taipei"
    ports:
      - "11211:11211"