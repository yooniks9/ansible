version: '3.1'

services:

  db:
    container_name: mariadb
    image: mariadb:{{ mysql_version }}
    restart: always
    ports:
      - 3306:3306
    volumes:
      - ./data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: {{ mysql_password }}